name: coordinator
version: 0.1.0
runtime: ./coordinator
description: Decompose complex tasks into parallel sub-tasks and dispatch to agents
capabilities:
  - tools
intents: [design, write]
triggers: [coordinate, decompose, parallel tasks, multi-agent, orchestrate, complex task]
defaults:
  - src: cmd/defaults/prompt.md
    dest: extensions/coordinator/prompt.md
