# Build artifacts
*.exe
*.dll
*.so
*.dylib
# CLI binaries (ignore root binaries, allow source directories)
/repomap
!/repomap/
/pipeline
!/pipeline/
/bulk
!/bulk/
/lspq
!/lspq/
# Pack binaries
/agent
!/agent/
/code
!/code/
/context
!/context/
/core
!/core/
/workflow
!/workflow/

# Scratch
.work/

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo

# Go workspace (local dev only)
go.work
go.work.sum

# Repomix output
repomix-output.md

# Override global cache/ ignore — this directory is a Go package, not a build artifact
!cache/
