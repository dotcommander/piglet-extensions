# Build artifacts
*.exe
*.dll
*.so
*.dylib
# CLI binaries (ignore root binaries, allow source directories)
/repomap
!/repomap/
/pipeline
!/pipeline/
/bulk
!/bulk/
/lspq
!/lspq/
# Pack binaries
/pack-core
/pack-agent
/pack-context
/pack-code
/pack-workflow

# Scratch
.work/

# macOS
.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo

# Go workspace (local dev only)
go.work
go.work.sum

# Repomix output
repomix-output.md

# Override global cache/ ignore — this directory is a Go package, not a build artifact
!cache/

# Additional CLI and pack binaries
/piglet-cron
/pack-cron
