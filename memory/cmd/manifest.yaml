name: memory
version: 0.2.0
runtime: ./memory
description: Per-project key-value fact store for persistent cross-session context
capabilities:
  - tools
  - commands
  - prompt
intents: [configure]
triggers: [remember, recall, fact, context, cross-session, persist]
